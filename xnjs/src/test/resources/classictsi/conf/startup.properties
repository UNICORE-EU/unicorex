#
# Basic properties for UNICORE TSI
#

export PATH=/bin:/usr/bin:/usr/local/bin

PYTHON="python3"

PY=src/test/resources/classictsi/lib

CONF=src/test/resources/classictsi/conf

PID=target/TSI_PID

date=`date +_%Y_%m_%d`
time=`date +_%H_%M`
mkdir -p target/tsi-logs

STARTLOG=target/tsi-logs/TSILog${date}${time}

